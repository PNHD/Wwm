#!/usr/bin/env node
import fs from 'node:fs';
const target=process.argv[2];if(!target)throw new Error('usage: node tools/instrument-candidate-recall-local-landscape.mjs <site/vision-sync.js>');let src=fs.readFileSync(target,'utf8');const anchor='window.__WWMSYNC_ABSOLUTE_SELFTEST__=selfTest;window.__WWMSYNC_ABSOLUTE_DRAW_TEST_CAPTURE__=drawTestCapture;';if(!src.includes(anchor))throw new Error('landscape anchor missing');
const code=String.raw`
function __landEval(source,field,x,y,radius,angle,step=2){const raw=structuralPhaseScoreFast(source,field,x,y,radius,angle),scale=scaleAwareStructuralScore(source,field,x,y,radius,angle,step),intensity=warpIntensityNccFast(source,field,x,y,radius,angle,step);return{x,y,radius,scale:radius/(ROI_N/2),angle,structure:raw.score,structureNcc:raw.ncc,scaleAwareStructure:scale.score,gradient:scale.gradient,topology:scale.topology,magnitude:scale.magnitude,edgeF1:scale.edgeF1,negative:scale.negative,sign:scale.sign,intensity:intensity.score,ncc:intensity.ncc,combined:.92*Math.max(0,scale.score)+.08*Math.max(0,intensity.score)}}
async function __WWMSYNC_LOCAL_LANDSCAPE(canvas,mapId,gt,tolerance=60){const cfg=MAPS[Number(mapId)],info=roiSamples(canvas),fine=await fineFieldAround(cfg,gt.x,gt.y,128,180),cx=gt.x-fine.minX*256,cy=gt.y-fine.minY*256;let exact=null;const poseScan=[];for(let radius=56;radius<=144;radius+=4){for(let angle=0;angle<360;angle+=5){const e=__landEval(info.field,fine.field,cx,cy,radius,angle,2);poseScan.push({radius,angle,scaleAwareStructure:e.scaleAwareStructure,intensity:e.intensity,combined:e.combined});if(!exact||e.combined>exact.combined)exact=e}await sleep(0)}const spatial=[];for(let dy=-120;dy<=120;dy+=12){for(let dx=-120;dx<=120;dx+=12){const e=__landEval(info.field,fine.field,cx+dx,cy+dy,exact.radius,exact.angle,2);spatial.push({...e,globalX:gt.x+dx,globalY:gt.y+dy,dx,dy,distance:Math.hypot(dx,dy)})}await sleep(0)}spatial.sort((a,b)=>b.combined-a.combined);const strongest=spatial[0],wrong=spatial.find(p=>p.distance>tolerance)||null,basin=spatial.filter(p=>p.combined>=strongest.combined-.03),rings=[];for(const [lo,hi] of [[0,12],[12,36],[36,60],[60,90],[90,130],[130,180]]){const pts=spatial.filter(p=>p.distance>=lo&&p.distance<hi);rings.push({minDistance:lo,maxDistance:hi,count:pts.length,meanCombined:pts.length?pts.reduce((s,p)=>s+p.combined,0)/pts.length:null,bestCombined:pts.length?Math.max(...pts.map(p=>p.combined)):null})}const scaleSensitivity=[];for(let factor=.70;factor<=1.3001;factor+=.05){const e=__landEval(info.field,fine.field,cx,cy,exact.radius*factor,exact.angle,2);scaleSensitivity.push({factor:Number(factor.toFixed(2)),radius:e.radius,structure:e.structure,scaleAwareStructure:e.scaleAwareStructure,intensity:e.intensity,ncc:e.ncc,combined:e.combined})}const rotationSensitivity=[];for(let da=-90;da<=90;da+=5){const e=__landEval(info.field,fine.field,cx,cy,exact.radius,angleNorm(exact.angle+da),2);rotationSensitivity.push({deltaAngle:da,angle:e.angle,structure:e.structure,scaleAwareStructure:e.scaleAwareStructure,intensity:e.intensity,ncc:e.ncc,combined:e.combined})}const gradBase=rings[0]?.meanCombined??exact.combined,grad60=rings.find(r=>r.minDistance===36)?.meanCombined??gradBase;return{boundedRegion:{center:gt,spatialHalfWidth:120,spatialStep:12,poseRadiusRange:[56,144],poseRadiusStep:4,poseAngleRange:[0,355],poseAngleStep:5},input:{positiveCount:info.positiveCount,negativeCount:info.negativeCount,normalizedContrast:info.contrast},expectedCoordinateBestPose:{...exact,globalX:gt.x,globalY:gt.y,gtDistance:0},strongestNearbyLocalMaximum:strongest,strongestWrongMaximum:wrong,distanceBetweenMaxima:wrong?Math.hypot(strongest.globalX-wrong.globalX,strongest.globalY-wrong.globalY):null,basinWidthApproxPx:basin.length?2*Math.max(...basin.map(p=>p.distance)):0,spatialGradientCombinedPerPixel:(gradBase-grad60)/48,radialRings:rings,scaleSensitivity,rotationSensitivity,poseScanCount:poseScan.length,spatialSampleCount:spatial.length}}
window.__WWMSYNC_CANDIDATE_RECALL_LOCAL_LANDSCAPE__={run:__WWMSYNC_LOCAL_LANDSCAPE};
`;
src=src.replace(anchor,code+'\n'+anchor);fs.writeFileSync(target,src);console.log(JSON.stringify({target,injected:true,diagnosticOnly:true,scoreSemanticsChanged:false,kind:'bounded-local-landscape'}));